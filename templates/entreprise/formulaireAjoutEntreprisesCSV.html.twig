{% extends 'base.html.twig' %}

{% block titrePage %}
    Formulaire d'ajout d'entreprises
{% endblock %}

{% block contenu %}
    <div id="contenuPage" class="container-fluid">
	    <div class="row">
            <div class="col-md-6 offset-md-3">
                    {{ form_start(vueFormulaireEntreprise) }}

                    <div> <!-- Affichage du champ de sélection de fichier CSV -->
                        {{form_row(vueFormulaireEntreprise.fichierCSV, {'label': "Fichier CSV"})}} <br/>
                    </div>

                    
                    <button type="submit" class="btn btn-dark">Enregistrer</button>

                    {{ form_end(vueFormulaireEntreprise) }}
            </div>
	    </div>
    </div>
{% endblock %}


